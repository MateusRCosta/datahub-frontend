export enum OPERADOR_ENUM {
  AND = 'and',
  OR = 'or',
  EQUAL = 'eq',
  DIFFERENT = 'df',
  GREATER = 'gt',
  LESS = 'lt',
  GREATER_EQUAL = 'gte',
  LESS_EQUAL = 'lte',
  CONTAINS = 'contains',
  START_WITH = 'startsWith',
  IS_NULL = 'isNull',
  IS_NOT_NULL = 'isNotNull',
}

export enum TIPO_FILTRO_ENUM {
  GROUP = 'group',
  FILTER = 'filter',
}

export enum OPERADOR_WHERE_ENUM {
  AND = 'and',
  OR = 'or',
}

export enum TIPO_JOIN_ENUM {
  INNER = 'INNER',
}
