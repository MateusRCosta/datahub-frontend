/**
 * Re-exporta ROLES, Role, ResourceName e helpers do config centralizado.
 * Este arquivo existe para manter compatibilidade com imports existentes.
 */
export { ROLES, RESOURCE_CONFIG } from "../config/resources";
export type { Role, ResourceName, ResourceEntry } from "../config/resources";
